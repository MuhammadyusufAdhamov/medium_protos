syntax="proto3";

package genproto;

option go_package="genproto/verify_service";

import "google/protobuf/empty.proto";

service VerificationService {
  rpc VerifyEmail(VerifyEmail) returns (google.protobuf.Empty) {}
}

message VerifyEmail {
  string email = 1;
  string password = 2;
}